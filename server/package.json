{
  "name": "winnow-server",
  "version": "0.1.0",
  "description": "Winnow deep-analysis server. Corpus-backed signals the local engine cannot compute alone.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "node --experimental-sqlite src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
